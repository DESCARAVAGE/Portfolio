<form method="post" action="{{ path('admin_profil_delete', {'id': profil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil.id) }}">
    <button class="btn">Delete</button>
</form>
